spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/price_processor
    username: price_user
    password: price_pass123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    locations: classpath:db/migration

app:
  folders:
    incoming: ./incoming      # сюда будем кидать прайсы
    processed: ./processed    # сюда положим готовые унифицированные файлы
    archive: ./archive        # архив исходников после обработки
    error: ./error            # если ошибка — сюда